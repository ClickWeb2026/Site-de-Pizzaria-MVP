generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Pizza {
  id           String @id
  name         String
  description  String
  price_small  Float
  price_medium Float
  price_large  Float
  image_url    String
  category     String
}